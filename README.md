# cm-staff-placement

건설사업관리용역 현장 직원 배치 관리 (프로토타입)

현장별 직원의 **계약배치 · 실제배치 · 변경 예정 배치**를 한 화면에서 확인하고,
엑셀 파일을 업로드하면 **중복 배치 인원을 자동으로 찾아주는** 웹 앱입니다.

> 모든 기능이 **브라우저 안에서만(완전 로컬) 동작**합니다.
> 데이터는 서버로 전송되지 않으며, 현재 탭의 브라우저 메모리에서만 처리됩니다.

---

## 1. 파일 받기

이 저장소를 다운로드하거나, git이 있으면 클론합니다.

```bash
git clone https://github.com/gittaeo/cm-staff-placement.git
cd cm-staff-placement
```

실행에는 `index.html`과 `app-fixes.js`가 모두 필요합니다. 두 파일을 같은 폴더에 보관하세요.

---

## 2. 실행 방법 (두 가지 중 하나)

### 방법 A. 파일을 바로 열기 (가장 간단)

- `index.html`을 **더블클릭**하면 브라우저에서 바로 실행됩니다.
- 설치할 것 없음, 인터넷 불필요, 회사 PC에서 그대로 사용 가능.

### 방법 B. cmd에서 로컬 서버로 실행 (URL로 접속)

1. **시작 → `cmd`** 실행 후, 파일이 있는 폴더로 이동:

```bash
cd 경로\cm-staff-placement
```

2. 아래 명령으로 로컬 서버 실행 (Python 필요):

```bash
python -m http.server 8233
```

3. 브라우저 주소창에 아래 주소 입력:

```
http://127.0.0.1:8233/
```

종료하려면 cmd 창에서 `Ctrl + C` 를 누르면 됩니다.

---

## 3. 사용 방법

1. **엑셀 업로드 · 중복 인원 스캔** — 전체 배치 파일(`.xlsx` / `.xlsm` / `.csv`)을 업로드
   - 단일·다중 파일과 여러 시트를 읽고, 일반 헤더 및 `계약서 기준/실배치 → 투입/철수` 형태의 다단 헤더를 자동 인식
   - 임포트한 파일 안에서 **중복 배치 인원**이 자동으로 표시됩니다
   - 누락·오류 행과 배치표가 아닌 시트는 `비배치·확인 필요 목록`에서 원본 파일·시트·행 번호와 사유 확인
2. **전체 배치 현황** — 현장별 직원 배치 타임라인, 계약/실제/변경 예정 구분, 중복 강조 표시
3. **배치 관리** — 현장·직원·배치 등록/수정/삭제, 변경 이력, 직원별 이력
4. **AI 점검·요약** — 중복 배치 · 배치 변경 내용 · 계약배치 vs 실제배치 차이를 요약
   - **완전 로컬 규칙 기반 분석**이며 외부 AI API로 데이터를 전송하지 않음
5. **엑셀 내려받기** — 선택한 직원·현장의 모든 원본 배치 행과 원본 열 전체를 `.xlsx`로 저장
   - 표준 날짜와 원본의 투입·철수 날짜를 모두 `YYYY-MM-DD`로 출력

---

## 4. 대외비 보안 주의 (중요)

- 데이터는 **현재 탭의 메모리에만 유지**되며 새로고침하거나 탭을 닫으면 삭제됩니다.
- **실제 업무 데이터 파일(엑셀, 내려받은 XML)은 이 저장소에 절대 커밋하지 마세요.**
  `git add` 하기 전에 업로드용 파일이 있는지 반드시 확인하세요.
- **GitHub Pages는 켜지 마세요.** Pages URL은 인터넷에 공개됩니다.
- 이 저장소는 **private**으로만 유지하세요.

---

## 5. 저장소 안 파일

| 파일 | 내용 |
| --- | --- |
| `index.html` | 앱 화면, 로컬 엑셀 판독기, 기본 기능과 예시 데이터 |
| `app-fixes.js` | 다중 파일 정규화, 다단 헤더, 검색·이력·타임라인, 전체 원본 엑셀 내려받기 보완 기능 |
| `README.md` | 사용·실행 안내 |

---

## 6. 변경 이력

### 2026-08-12

- `계약서 기준/실배치 → 투입/철수` 다단 헤더 엑셀 자동 인식
- 계약배치와 실제배치를 각각 독립된 배치 행으로 정규화
- 배치표가 아닌 시트와 제외 행을 `비배치·확인 필요 목록`에서 조회
- 원본 Excel 행 번호 보존
- 직원명 한글 입력과 복합 검색 필터 수정
- 계약·실제·변경 예정 타임라인 색상 및 중복 배치 빨간색 강조 수정
- 중복 배치 0건에서도 전체 배치 현황 유지
- 직원별 이력의 `undefined` 표시 문제 수정
- 직원별·현장별 엑셀에 모든 원본 행과 원본 열의 합집합 포함
- 원본 투입·철수 Excel 일련번호를 `YYYY-MM-DD`로 변환
- 브라우저 저장소와 외부 AI 전송을 제거하고 메모리 전용 로컬 처리 적용
